import { Common } from './common';

export const SPR = {
    ...Common,

    'general_parameters/technique': {
        defaultValue: 'Surface plasmon resonance (SPR)',
        dontDisplay: true,
    },
};
